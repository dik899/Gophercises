package main

import (
	"testing"

)

// TestMain is utility function
// MAKE THIS A util functiontion in utils and then use everywhere :)
// Make pagination a utils function and do the same (and doc it) move the model there as well
func TestMainApp(t *testing.T) {
	main()	
}
